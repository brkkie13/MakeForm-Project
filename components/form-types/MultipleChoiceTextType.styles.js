'use client';
import styled from 'styled-components';

export const OptionsWrapper = styled.div`
  display: grid;
  grid: '. .';
  gap: 18px;
  margin-bottom: 15px;

  .option {
    position: relative;
  }
`;
