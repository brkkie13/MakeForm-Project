'use client';
import styled from 'styled-components';

export const OptionsWrapper = styled.div`
  display: grid;
  grid: '. .';
  gap: 20px;
  margin: 0px 10px;
  margin-bottom: 22px;

  .option {
    position: relative;
  }
`;
